---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Inicio | Sitio Genérico" description="Página moderna, adaptable y con diseño limpio.">
  <!-- Sección Hero -->
  <section
    id="inicio"
    class="relative min-h-screen flex flex-col items-center justify-center text-center bg-fixed bg-cover bg-center"
    style="background-image: url('/fondo.jpg');"
  >
    <div class="absolute inset-0 bg-[color:var(--color-secondary)]/80 backdrop-blur-sm"></div>

    <div class="relative z-10 px-6">
      <h1 class="text-4xl sm:text-6xl font-bold text-[color:var(--color-primary)] mb-6 drop-shadow-lg">
        Bienvenido a nuestro espacio digital
      </h1>
      <p class="text-[color:var(--color-text)]/90 max-w-2xl mx-auto text-lg leading-relaxed mb-8">
        Un sitio moderno, claro y adaptable a cualquier tipo de contenido o proyecto.
      </p>
      <a
        href="#contacto"
        class="bg-[color:var(--color-primary)] text-[color:var(--color-secondary)] font-semibold px-8 py-3 rounded-full shadow-md hover:scale-105 transition"
      >
        Saber más
      </a>
    </div>
  </section>

  <!-- Sección de características -->
  <section id="caracteristicas" class="py-24 bg-[color:var(--color-secondary)] text-[color:var(--color-text)]">
    <div class="max-w-6xl mx-auto px-6 text-center">
      <h2 class="text-3xl font-semibold mb-12">Características principales</h2>
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-10">
        <div class="p-6 bg-white/5 rounded-2xl shadow hover:scale-105 transition">
          <h3 class="text-xl font-semibold text-[color:var(--color-primary)] mb-2">Diseño adaptable</h3>
          <p class="text-sm text-[color:var(--color-text)]/80">
            La estructura se ajusta automáticamente a cualquier tamaño de pantalla o dispositivo.
          </p>
        </div>
        <div class="p-6 bg-white/5 rounded-2xl shadow hover:scale-105 transition">
          <h3 class="text-xl font-semibold text-[color:var(--color-primary)] mb-2">Contenido flexible</h3>
          <p class="text-sm text-[color:var(--color-text)]/80">
            Se puede adaptar fácilmente para mostrar información, productos o proyectos.
          </p>
        </div>
        <div class="p-6 bg-white/5 rounded-2xl shadow hover:scale-105 transition">
          <h3 class="text-xl font-semibold text-[color:var(--color-primary)] mb-2">Fácil personalización</h3>
          <p class="text-sm text-[color:var(--color-text)]/80">
            Los colores, textos y secciones pueden ajustarse con simples cambios en las variables.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección visual / galería -->
  <section id="galeria" class="py-24 bg-[color:var(--color-secondary)]/60 text-[color:var(--color-text)] backdrop-blur-sm">
    <div class="max-w-6xl mx-auto px-6 text-center">
      <h2 class="text-3xl font-semibold mb-12">Galería de ejemplos</h2>
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-8">
        <div class="rounded-2xl overflow-hidden shadow-lg hover:scale-105 transition">
          <img src="/imagen1.jpg" alt="Imagen 1" class="w-full h-56 object-cover" />
        </div>
        <div class="rounded-2xl overflow-hidden shadow-lg hover:scale-105 transition">
          <img src="/imagen2.jpg" alt="Imagen 2" class="w-full h-56 object-cover" />
        </div>
        <div class="rounded-2xl overflow-hidden shadow-lg hover:scale-105 transition">
          <img src="/imagen3.jpg" alt="Imagen 3" class="w-full h-56 object-cover" />
        </div>
      </div>
    </div>
  </section>

  <!-- Sección informativa -->
  <section id="informacion" class="py-24 bg-[color:var(--color-secondary)] text-[color:var(--color-text)]">
    <div class="max-w-5xl mx-auto px-6 text-center">
      <h2 class="text-3xl font-semibold mb-6">Información general</h2>
      <p class="text-[color:var(--color-text)]/80 leading-relaxed max-w-3xl mx-auto">
        Este diseño fue creado como una plantilla base, ideal para proyectos personales, demostraciones o presentaciones
        profesionales.  
        Su estructura clara permite destacar cualquier tipo de contenido sin distraer al usuario.
      </p>
    </div>
  </section>

  <!-- Sección de contacto -->
  <section id="contacto" class="py-24 bg-[color:var(--color-secondary)]/80 backdrop-blur-md text-[color:var(--color-text)]">
    <div class="max-w-4xl mx-auto px-6 text-center">
      <h2 class="text-3xl font-semibold mb-6">Contacto</h2>
      <p class="mb-8 text-[color:var(--color-text)]/80">
        Si deseas más información o querés personalizar esta estructura, podés dejar un mensaje.
      </p>

      <form class="max-w-md mx-auto grid gap-4">
        <input type="text" placeholder="Tu nombre" class="w-full p-3 rounded-md bg-white/10 text-white placeholder-white/60 focus:outline-none focus:ring-2 focus:ring-[color:var(--color-primary)]" />
        <input type="email" placeholder="Tu correo" class="w-full p-3 rounded-md bg-white/10 text-white placeholder-white/60 focus:outline-none focus:ring-2 focus:ring-[color:var(--color-primary)]" />
        <textarea rows="4" placeholder="Tu mensaje" class="w-full p-3 rounded-md bg-white/10 text-white placeholder-white/60 focus:outline-none focus:ring-2 focus:ring-[color:var(--color-primary)]"></textarea>
        <button class="bg-[color:var(--color-primary)] text-[color:var(--color-secondary)] font-semibold py-3 rounded-full shadow-md hover:scale-105 transition">
          Enviar mensaje
        </button>
      </form>
    </div>
  </section>
</Layout>
